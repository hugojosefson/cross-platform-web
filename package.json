{
  "private": true,
  "name": "cross-platform",
  "version": "0.0.0",
  "description": "Cross-platform JS web project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run deps && node server/server.js",
    "deps": "npm install && bower install"
  },
  "dependencies": {
    "colors": "^0.6.2",
    "express": "^4.4.3"
  },
  "devDependencies": {
    "bower": "^1.3.5"
  },
  "author": "Hugo Josefson <hugo@josefson.org>",
  "license": "MIT"
}
